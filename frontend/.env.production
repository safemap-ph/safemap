# SafeMap-PH Frontend — Production Build
# Vite replaces import.meta.env.* at build time with these values.
#
# Set VITE_API_BASE_URL to your PythonAnywhere backend URL.
# All /api/* fetch calls in the app will be prefixed with this value.
VITE_API_BASE_URL=https://safemap.pythonanywhere.com/api
